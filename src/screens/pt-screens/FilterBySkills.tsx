import React from 'react';
import { View, Text } from 'react-native';

const FilterBySkills = () => {
	return (
		<View>
			<Text>FilterBySkills</Text>
		</View>
	);
};

export default FilterBySkills;
