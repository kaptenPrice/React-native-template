import React from 'react';
import { View, Text } from 'react-native';

const PTFilterScreen = () => {
	return (
		<View>
			<Text>PTFilterScreen</Text>
		</View>
	);
};

export default PTFilterScreen;
