/*import { TypedNavigator } from '@react-navigation/core';
import {
	NativeStackNavigationEventMap,
	NativeStackNavigationOptions,
	NativeStackNavigatorProps,
} from '@react-navigation/native-stack/lib/typescript/src/types';
import { ParamListBase, StackNavigationState } from '@react-navigation/routers';
const handleMapScreens = (screens, stack): myProps => {
	return screens.map(({ ...props }) => <stack.screen key={index} {...props} />);
};

type myProps = TypedNavigator<
	ParamListBase,
	StackNavigationState<ParamListBase>,
	NativeStackNavigationOptions,
	NativeStackNavigationEventMap,
	({
		initialRouteName,
		children,
		screenListeners,
		screenOptions,
		...rest
	}: NativeStackNavigatorProps) => JSX.Element
>;
*/